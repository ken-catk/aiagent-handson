# 会食ムキムキ君 × グルメ MCP PRD（v1）

> 旧「Junk Food AI Agent」要件から、業務目的を「筋肉づくり/健康維持に配慮した会食候補の提案」へ変更した版。

## 1. 目的（短く）

- グルメ検索API（グルメサーチ）を MCP でラップし、AI が MCP 経由で安全に店舗検索できる状態を作る
- ローカル Docker 上の AI Agent「会食ムキムキ君」が MCP を呼び、健康的な会食に向いた候補を返す
- V7ハンズオン構成（MCP作成 -> Agent作成 -> 連携テスト）で実装可能な設計にする

---

## 2. 想定ユースケース

1. ユーザーが自然言語で「沖縄で会食したい条件」を入力
2. Agent が MCP ツールで グルメ検索API を検索
3. Agent が検索結果を「健康会食候補判定ロジック（単語一致）」でフィルタ
4. 健康的な料理が期待できる候補のみ、理由つきで返す（最大5件）

例:

- 入力: `沖縄で会食向けに、タンパク質が取りやすいお店を教えて`
- 出力: `候補店舗 + ヒットした健康キーワード + 推薦理由 + 参照URL`

---

## 3. スコープ（MVP）

### In Scope

- MCP Server（グルメ検索API ラッパー）
- AI Agent（Responses API 呼び出し + MCP呼び出し + 判定ロジック）
- 連携テスト（正常系 / 異常系）

### Out of Scope（MVPではやらない）

- ユーザー認証
- 永続DB
- 栄養価の厳密計算（カロリー/マクロ栄養素計算）
- 学習ベース推薦（機械学習）

---

## 4. アーキテクチャ（V7準拠）

```text
[User]
  -> [Agent Container (TypeScript): 会食ムキムキ君]
      -> OpenAI Responses API (LLM)
      -> [MCP Container (TypeScript)]
          -> グルメ検索API
```

- ローカル実行: Docker Compose
- LLMのみ外部（OpenAI API）
- Agent と MCP は分離デプロイ（責務分離）

---

## 5. 技術スタック（最小構成）

### 実行環境（2026-04-24 時点）


| 項目               | バージョン                     | 備考               |
| ---------------- | ------------------------- | ---------------- |
| Node.js          | **24.15.0 (LTS Krypton)** | `.mise.toml` で固定 |
| npm              | **11.12.1**               | Node 24 同梱       |
| TypeScript       | **5.7+** （6.0系も可）         | ハンズオンは5.x推奨      |
| Docker / Compose | 最新安定版                     | 受講者PCが古い場合は更新    |


### ライブラリ

- `@modelcontextprotocol/sdk`（MCPサーバー実装）
- `openai`（Responses API呼び出し）
- `zod`（入力/出力スキーマ検証）
- `dotenv`（環境変数管理）

### 追加ライブラリを使わない方針

- ログ出力: `console.log(JSON.stringify(...))` で構造化ログ
- グルメ検索API呼び出し: Node.js 組み込み `fetch`

---

## 6. MCP 設計（MVP）

### MCPツール

1. `search_shops`
  - 役割: 沖縄エリア内で店舗一覧を検索（キーワード、予算、会食条件）
2. `get_shop_detail`
  - 役割: 店舗詳細を追加取得（判定補助・最終表示用）

### 重要方針

- ツール説明に「使いどころ」を明記
- `tools/list` は固定順
- `tools/call` エラー形式を統一（`code/message/details`）

---

## 7. Agent 設計（LLMとコードの責務分離）

### LLMが担当する処理

- ユーザー入力の意図解釈
- どのMCPツールを呼ぶかの選択
- 最終回答の自然言語整形

### Agent（コード）が担当する処理

- ツール実行ループ制御（`MAX_TURNS`）
- MCP実行結果の検証
- 健康会食候補判定ロジックの決定的適用
- エラー時の再試行/中断制御
- ログ記録（連携テスト・デバッグ用）

### Responses API 採用時の実装ポイント

- Chat Completions と入出力形状が異なるため、`agent.ts` は Responses API前提で実装

---

## 8. 健康会食候補判定ロジック（MVP）

### 判定方式

- 単語一致ベース（決定的）
- 判定対象: `店舗名 / ジャンル / キャッチ`

### 判定対象フィールドの説明（受講者向け）

- `店舗名`: 店の正式名称（例: ○○鶏料理）
- `ジャンル`: 料理カテゴリ（例: 焼き鳥、魚介、しゃぶしゃぶ）
- `キャッチ`: 店舗の短い紹介コピー（グルメ上の訴求文）

補足:

- まずはこの3項目だけを見る（判定を分かりやすく保つため）
- 長文説明文はMVPでは対象外（誤判定と実装コストを抑える）

### 健康キーワード辞書（初期案）

- 高タンパク系: `高たんぱく`, `タンパク質`, `鶏むね`, `赤身`, `魚介`, `刺身`
- 低脂質/調理法系: `グリル`, `蒸し`, `茹で`, `炙り`, `低脂質`
- ヘルシー訴求系: `ヘルシー`, `サラダ`, `野菜`, `糖質オフ`, `健康志向`

優先順位:

- `高タンパク > 低脂質 > 野菜系` の順で評価する

### 除外キーワード辞書（初期案）

- `食べ放題`, `デカ盛り`, `こってり`

### 出力ルール

- ヒット単語を必ず理由として返す
- 0件の場合は「条件に一致する健康会食候補なし」を返す
- 返却件数は最大5件
- 「筋トレ観点コメント（高タンパク/低脂質の観点）」を必ず付ける

---

## 9. 受け入れ条件（MVP完了ライン）

1. Agent から MCP 経由で グルメ検索API 呼び出しが成功する
2. 検索対象エリア（沖縄全体）で候補が取得できる
3. 健康キーワード一致に基づき候補が正しくフィルタされる
4. `search_shops` と `get_shop_detail` の両ツールが利用される
5. 1回答あたり最大5件で返る
6. 各候補に筋トレ観点コメントが付与される
7. 連携テストで以下が確認できる
  - 正常系: 候補が返る
  - 異常系: APIエラー時に中断/エラー整形される
  - 異常系: 不正入力を弾ける

---

## 10. 決定事項（確定）

1. グルメ検索API は `グルメサーチ` を使う
2. 検索対象エリアは `沖縄全体`
3. 判定方式は `単語一致のみ`
4. 判定対象は `店舗名 / ジャンル / キャッチ`
5. 1回の回答で返す件数上限は `5件`
6. MCPツールは `search_shops` と `get_shop_detail` をMVPで入れる
7. OpenAI API は `Responses API` を採用する
8. ログはローカルファイル（JSON Lines）に保存する
9. Node.js は `.mise.toml` の固定版を使う
10. 健康キーワード辞書の優先順位は `高タンパク > 低脂質 > 野菜系`
11. 除外キーワード辞書を導入する（`食べ放題`, `デカ盛り`, `こってり`）
12. 会食向け条件（`個室あり` / `静かな席`）はMVPで入れない
13. 価格帯は固定しない
14. 出力に筋トレ観点コメントを必須にする

---

## 11. ログを使う目的（なぜ必要か）

### Agentログで見ること

- どのツールを選んだか
- どの引数で呼んだか
- 何ターン目で成功/失敗したか

### MCPログで見ること

- グルメ検索APIへの実リクエスト内容
- APIエラー原因（認証/パラメータ/レート制限）
- 返却件数（0件理由の切り分け）

### 連携テストでの使い方

- 正常系/異常系の失敗箇所を即特定する
- 再現性のある不具合報告を残す

---

## 12. 環境変数


| キー                  | 用途                                  | 例                          |
| ------------------- | ----------------------------------- | -------------------------- |
| `OPENAI_API_KEY`    | OpenAI API 認証                       | `sk-...`                   |
| `OPENAI_MODEL`      | 使用モデル                               | `gpt-4.1-mini` / `gpt-5.2` |
| `GOURMET_API_KEY` | グルメ検索API 認証                    | `xxxxxxxxxxxxxxxx`         |
| `MCP_AUTH_TOKEN`    | MCPサーバー側の認証トークン                     | `openssl rand -hex 24`     |
| `AGENT_MCP_TOKEN`   | Agent側から送る認証トークン（MCP_AUTH_TOKENと同値） | 同上                         |
| `LOG_DIR`           | ログ出力先                               | `./logs`                   |


---

## 13. 回答反映メモ（2026-04-24）

1. 健康キーワード辞書の優先順位は `高タンパク > 低脂質 > 野菜系` で確定
2. 除外キーワードは導入する（`食べ放題`, `デカ盛り`, `こってり`）
3. `個室あり` / `静かな席` はMVP対象外
4. 価格帯は固定しない
5. 出力時の筋トレ観点コメントは必須
